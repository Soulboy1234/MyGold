# Agent4-定投选手

这是一个以 Agent1 当前资产和历史记录为模板复制出的定投型黄金投资 Agent。

当前目录包含：
- `agent.mjs`：Agent4 的个体策略与执行逻辑入口
- `agent.json`：Agent4 元数据
- `out/`：Agent4 独立输出数据

当前策略：
- 保留复制时点的持仓和现金状态
- 从该时点开始改为每天下午 `15:00` 定投 `1000` 元黄金
- 当剩余现金不足 `1000` 元时，最后一次投入全部剩余现金
- 当扣除手续费后的净盈利达到目标且价格过热时，会低频止盈
- 当长期趋势明显转弱时，会做防守减仓
