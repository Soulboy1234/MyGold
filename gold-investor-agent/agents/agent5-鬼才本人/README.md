# Agent5-鬼才本人

这是一个以 Agent4 当前资产和历史记录为模板复制出来的纯手动黄金投资 Agent。

当前目录包含：
- `agent.mjs`：Agent5 的个体策略与执行逻辑入口
- `agent.json`：Agent5 元数据
- `out/`：Agent5 独立输出数据

当前规则：
- 保留复制时点的持仓和现金状态
- 不自动买入
- 不自动卖出
- 每次后续交易都由你单独下指令后再执行
- 平时运行时只刷新估值和面板数据
